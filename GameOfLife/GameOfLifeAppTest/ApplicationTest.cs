using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace GameOfLifeAppTest
{
    [TestClass]
    public class ApplicationTest
    {
        [TestMethod]
        public void TestPrimaryScenario()
        {
            // GameOfLifeMockView mockApp = new ...
            // Assert mockApp.File is null
            // Assert mockApp.Image is zero-sized
            // Assert that Next(), Start(), Stop(), Reset() do not change anything
            // mockApp.File = test input file
            // Assert mockApp.Image has changed
            // Assert mockApp.Image equals to the expected one
            // Assert when mockApp.Next() then Image equals to the expected one
            // Assert when mockApp.Reset() then Image equals to the original one
        }
    }
}
